@AGENTS.md

Claude-specific notes: use `qtk-preview --dump`/`--grab` (see AGENTS.md)
to verify every visual claim before reporting it done, keep QML files
complete at every write, and never reduce build parallelism.
